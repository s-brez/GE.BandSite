@page
@model GE.BandSite.Server.Pages.About.IndexModel
@{
    ViewData["Title"] = "About";
}

<div class="page" style="padding-top: 48px;">

    <section class="page__section about-hero">
        <div class="about-hero__copy">
            <p class="page-eyebrow">Our Story</p>
            <p>Swing The Boogie is more than a band – it’s a performance experience. Led by charismatic vocalist Gilbert Ernest, we bring the timeless spirit of swing and jazz into today’s celebrations. From Melbourne to Madrid, New York to New Delhi, our music has filled ballrooms, weddings, and festivals across the world.</p>
        </div>
        <div class="about-hero__facts">
            @foreach (var fact in Model.SpotlightFacts)
            {
                <article class="fact-card">
                    <span class="fact-card__heading">@fact.Heading</span>
                    @if (!string.IsNullOrWhiteSpace(fact.Detail))
                    {
                        <span class="fact-card__detail">@fact.Detail</span>
                    }
                </article>
            }
        </div>
    </section>

    <figure class="about-story__feature-image">
        <img src="https://d2r0vyil5uhr44.cloudfront.net/images/12_web.jpg" alt="Swing The Boogie performing." />
    </figure>

    <section class="page__section about-story">
        <h2 class="about-story__subheading">Why Choose Us</h2>
        <div class="about-story__grid">
            <div class="about-story__narrative">
                @foreach (var paragraph in Model.StoryParagraphs)
                {
                    <p>@paragraph</p>
                }
            </div>
            <div class="about-story__gallery">
                @foreach (var image in Model.ShowcaseImages)
                {
                    <figure class="about-story__image">
                        <img src="@image.Path" alt="@image.AltText" />
                    </figure>
                }
            </div>
        </div>
    </section>

</div>
